

export enum Types {
  string,
  text,
  integer,
  bigint,
  float,
  real,
  double,
  decimal,
  date,
  dateonly,
  boolean,
  enum,
  uuid,
  virtual,

  //PostgreSQL only:
  array,
  json,
  jsonb,
  blob,
  range,
  geometry
}
