

module.exports = {
   connections: {
      db: {
         host: 'localhost',
         port: 3306,
         
         name: 'dev'
      }
   }
}

//In local-development.js, add the keys connections.db.user and connections.db.password
